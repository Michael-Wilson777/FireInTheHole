import { Container, Button } from 'reactstrap';

const HomePage = () => {
    return(
        <Container>
            <h1>Fire in the Hole</h1>
            <Button href='/product-page'>Enter</Button>
        </Container>

    );
};

export default HomePage;